# Pinned constraints for reproducible installs.
#
# requirements.txt declares supported lower bounds; this file pins the exact
# versions verified to pass the full test suite. Install reproducibly with:
#
#     pip install -r requirements.txt -c constraints.txt
#
# Regenerate after intentional upgrades:
#
#     pip freeze | grep -iE '^(pandas|openpyxl|requests|Flask|Werkzeug|urllib3|python-dotenv|numpy|scipy|pyproj|shapely|geopy|polyline|folium|cachetools|defusedxml)==' | sort
#
# Rationale: pandas 3.x changed mixed-timezone parsing behaviour (naive values
# coerced to NaT / "Mixed timezones" errors). Pinning guards against silent
# regressions from transitive upgrades.

Flask==3.1.3
Werkzeug==3.1.8
cachetools==7.0.5
defusedxml==0.7.1
folium==0.20.0
geopy==2.4.1
numpy==2.4.4
openpyxl==3.1.5
pandas==3.0.2
polyline==2.0.4
pyproj==3.7.2
python-dotenv==1.2.2
requests==2.33.1
scipy==1.17.1
shapely==2.1.2
urllib3==2.6.3
